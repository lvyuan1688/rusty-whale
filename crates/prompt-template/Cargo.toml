[package]
name = "prompt-template"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Templated system prompts for rusty-whale"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
