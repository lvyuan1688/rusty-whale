[package]
name = "error-context"
version = "0.1.8"
edition = "2021"
license = "MIT"
description = "Structured error context propagation for rusty-whale"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
